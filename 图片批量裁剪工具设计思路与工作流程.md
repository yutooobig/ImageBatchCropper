# 图片批量裁剪工具 - 设计思路与工作流程

## 项目概述
开发一个Windows平台的Python工具，用于批量裁剪图片大小，支持自定义裁剪方向和尺寸，通过拖放操作实现批量处理。

## 功能需求

### 核心功能
1. **拖放文件支持**：支持将图片文件或文件夹拖放到界面
2. **百分比裁剪参数**：
   - 上边裁剪百分比（%）
   - 下边裁剪百分比（%）
   - 左边裁剪百分比（%）
   - 右边裁剪百分比（%）
   - 示例：裁掉下方5% → 下边输入5
3. **批量处理**：支持多张图片同时处理
4. **输出设置**：
   - 选择输出文件夹
   - 文件命名规则设置
   - 输出格式选择（JPG、PNG等）

### 辅助功能
1. **预览功能**：裁剪前后预览
2. **进度显示**：处理进度条和状态显示
3. **错误处理**：无效文件提示和处理失败重试
4. **设置保存**：保存用户偏好设置

## 技术架构

### 开发环境
- **Python版本**：3.8+
- **GUI框架**：Tkinter（内置，无需额外安装）
- **图像处理库**：Pillow（PIL）
- **打包工具**：PyInstaller（生成exe可执行文件）

### 项目结构
```
ImageBatchCropper/
├── main.py              # 主程序入口
├── gui.py               # 图形界面模块
├── image_processor.py   # 图片处理核心逻辑
├── file_handler.py      # 文件操作模块
├── config.py            # 配置管理
├── utils.py             # 工具函数
└── requirements.txt     # 依赖包列表
```

## 工作流程

### 第一阶段：基础架构搭建（1-2天）
1. **项目初始化**
   - 创建项目目录结构
   - 设置虚拟环境
   - 安装依赖包（Pillow等）

2. **GUI界面设计**
   - 主窗口布局设计
   - 参数设置区域
   - 文件列表显示区域
   - 控制按钮区域

### 第二阶段：核心功能实现（2-3天）
1. **图片处理模块**
   - 实现图片加载和格式识别
   - 裁剪算法实现（居中、指定位置等）
   - 图片保存功能

2. **文件操作模块**
   - 拖放文件检测
   - 文件格式验证
   - 批量文件处理队列

### 第三阶段：功能完善（1-2天）
1. **用户交互优化**
   - 预览功能实现
   - 进度显示和状态更新
   - 错误提示和日志记录

2. **设置管理**
   - 配置文件读写
   - 默认参数设置
   - 用户偏好保存

### 第四阶段：测试和打包（1天）
1. **功能测试**
   - 各种图片格式测试
   - 边界条件测试
   - 性能压力测试

2. **打包发布**
   - 使用PyInstaller打包为exe
   - 创建安装程序
   - 文档编写

## 详细设计

### GUI界面布局
```
┌─────────────────────────────────────────────┐
│ 图片批量裁剪工具 v1.0                       │
├─────────────────────────────────────────────┤
│ 裁剪设置（百分比）:                         │
│ 上边裁剪: [  0] %   下边裁剪: [  5] %      │
│ 左边裁剪: [  0] %   右边裁剪: [  0] %      │
│                                             │
│ 输出设置:                                   │
│ 输出格式: [JPG]  质量: [90]%               │
│ 输出文件夹: [浏览...] [当前文件夹]          │
├─────────────────────────────────────────────┤
│ 文件列表:                                   │
│ ┌─────────────────────────────────────────┐ │
│ │ 1. image1.jpg (1024x768)               │ │
│ │ 2. image2.png (800x600)                │ │
│ │ 3. photo3.bmp (1920x1080)              │ │
│ └─────────────────────────────────────────┘ │
│ 拖放图片或文件夹到此区域                    │
├─────────────────────────────────────────────┤
│ 进度: ████████████████████ 75%             │
│ 状态: 正在处理 image3.jpg...               │
├─────────────────────────────────────────────┤
│ [开始裁剪] [清空列表] [退出]               │
└─────────────────────────────────────────────┘
```

### 核心算法设计

#### 百分比裁剪算法
```python
def crop_image_by_percentage(image, top_percent=0, bottom_percent=0, left_percent=0, right_percent=0):
    """
    按百分比裁剪图片
    
    Args:
        image: PIL Image对象
        top_percent: 上边裁剪百分比（0-100）
        bottom_percent: 下边裁剪百分比（0-100）
        left_percent: 左边裁剪百分比（0-100）
        right_percent: 右边裁剪百分比（0-100）
    
    Returns:
        cropped_image: 裁剪后的图片
    """
    # 获取原图尺寸
    img_width, img_height = image.size
    
    # 计算裁剪区域（像素值）
    top_pixels = int(img_height * top_percent / 100)
    bottom_pixels = int(img_height * bottom_percent / 100)
    left_pixels = int(img_width * left_percent / 100)
    right_pixels = int(img_width * right_percent / 100)
    
    # 计算裁剪后的尺寸
    new_width = img_width - left_pixels - right_pixels
    new_height = img_height - top_pixels - bottom_pixels
    
    # 验证裁剪后尺寸是否有效
    if new_width <= 0 or new_height <= 0:
        raise ValueError("裁剪参数过大，裁剪后图片尺寸无效")
    
    # 计算裁剪区域坐标
    left = left_pixels
    top = top_pixels
    right = img_width - right_pixels
    bottom = img_height - bottom_pixels
    
    # 执行裁剪
    return image.crop((left, top, right, bottom))
```

## 开发优先级

### 高优先级（必须实现）
1. 百分比裁剪功能（上下左右四个方向）
2. 拖放文件支持
3. 简单的GUI界面（四个百分比输入框）
4. 批量处理能力

### 中优先级（建议实现）
1. 预览功能
2. 进度显示
3. 错误处理
4. 设置保存

### 低优先级（可选实现）
1. 多线程处理
2. 高级裁剪选项
3. 图片滤镜效果
4. 批量重命名

## 风险评估

### 技术风险
1. **图片格式兼容性**：不同格式的图片处理可能存在问题
   - 解决方案：使用Pillow库的广泛格式支持

2. **大文件处理性能**：大尺寸图片可能处理缓慢
   - 解决方案：实现进度显示和可中断处理

3. **内存使用**：批量处理大量图片可能占用大量内存
   - 解决方案：分批次处理，及时释放资源

### 用户体验风险
1. **操作复杂性**：参数设置可能过于复杂
   - 解决方案：提供合理的默认值和预设选项

## 验收标准

### 功能验收
- [ ] 支持常见图片格式（JPG、PNG、BMP等）
- [ ] 拖放操作正常工作
- [ ] 裁剪结果符合预期
- [ ] 批量处理功能稳定
- [ ] 错误处理机制完善

### 性能验收
- [ ] 处理100张图片时间在合理范围内
- [ ] 内存使用稳定，无泄漏
- [ ] 界面响应流畅

### 用户体验验收
- [ ] 界面布局合理直观
- [ ] 操作流程简单明了
- [ ] 提示信息清晰准确

## 后续优化方向

1. **功能扩展**
   - 支持更多图片格式
   - 添加图片旋转、滤镜等功能
   - 支持命令行模式

2. **性能优化**
   - 多线程并行处理
   - 图片处理算法优化
   - 内存使用优化

3. **用户体验优化**
   - 皮肤主题支持
   - 快捷键支持
   - 国际化支持

---

*此文档为初步设计，具体实现细节可根据实际开发情况进行调整。*